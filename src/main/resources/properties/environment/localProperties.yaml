api:
  id: '0000'

environment:
  logging:
    level: 'DEBUG'

http:
  port: '8082'
  
  
db_config:
  host: 'localhost'
  port: '3306'
  user: 'root'
  db_name: 'user'
  
cron:
  expression: '0 0/5 * 1/1 * ? *'
  time_zone: 'Europe/Zagreb'